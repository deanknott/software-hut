-# This view will re-render the edit modal, show an error message
-# and scroll to the top

$('#modalWindow').html('#{j render template: 'members/edit'}');
$.flashAlert('An Error Occurred. Please Try Again', 'alert-alert');
$(this).scrollTop(0);
